{
  "name": "vk-chat-archiver",
  "version": "0.1.0",
  "description": "Offload every VK (vk.com) conversation you have: full message history plus photos, videos, voice messages, documents and stickers, saved locally as JSON + browsable HTML.",
  "type": "module",
  "bin": {
    "vk-archive": "bin/vk-archive.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node bin/vk-archive.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "license": "MIT",
  "private": true
}
